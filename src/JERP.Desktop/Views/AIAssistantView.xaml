<UserControl x:Class="JERP.Desktop.Views.AIAssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="🤖 AI Assistant" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Powered by Claude" FontSize="14" Opacity="0.7" Margin="0,5,0,0"/>
            </StackPanel>
            
            <Button Grid.Column="1" Content="Clear Chat" Command="{Binding ClearChatCommand}" 
                    Height="35" MinWidth="100" VerticalAlignment="Center"/>
        </Grid>

        <!-- Messages Area -->
        <Border Grid.Row="1" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" 
                BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
            <ScrollViewer x:Name="MessageScrollViewer" VerticalScrollBarVisibility="Auto" 
                          Padding="15">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,15">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsAssistant}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border MaxWidth="600" CornerRadius="12" Padding="15">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Background" Value="#2563EB"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsAssistant}" Value="True">
                                                    <Setter Property="Background" Value="#374151"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    
                                    <StackPanel>
                                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" 
                                                   Foreground="White" FontSize="14"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" 
                                                   FontSize="11" Opacity="0.7" Margin="0,5,0,0" 
                                                   Foreground="White"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" Text="{Binding UserQuestion, UpdateSourceTrigger=PropertyChanged}" 
                     Height="50" VerticalContentAlignment="Center" Padding="15"
                     ui:ControlHelper.PlaceholderText="Ask me anything about JERP, accounting, or compliance...">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            
            <Button Grid.Column="1" Command="{Binding SendMessageCommand}" 
                    Height="50" Width="120" Margin="10,0,0,0"
                    IsEnabled="{Binding IsSending, Converter={StaticResource InverseBooleanConverter}}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Content" Value="Send"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSending}" Value="True">
                                <Setter Property="Content" Value="Thinking..."/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        
        <!-- Inverse Boolean Converter -->
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"
            xmlns:local="clr-namespace:JERP.Desktop.Converters"/>
    </UserControl.Resources>
</UserControl>
